@page "{id:int}"
@model ClinicBooking.Web.Pages.Specializations.EditModel
@{
    ViewData["Title"] = "Modifica specializzazione";
}
<h2>Modifica specializzazione</h2>

<form method="post" class="mt-3">
    <input type="hidden" asp-for="Input.SpecializationId" />

    <div class="mb-3">
        <label asp-for="Input.Name" class="form-label"></label>
        <input asp-for="Input.Name" class="form-control" />
        <span asp-validation-for="Input.Name" class="text-danger"></span>
    </div>

    <button class="btn btn-primary">Salva</button>
    <a class="btn btn-secondary" asp-page="Index">Indietro</a>
</form>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}